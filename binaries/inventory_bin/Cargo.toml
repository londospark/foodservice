[package]
name = "inventory_bin"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { workspace = true, features = ["http1", "tokio"] , default-features = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] , default-features = true }
